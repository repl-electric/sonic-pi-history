MAX_POINTS = 10

live_loop :graphics do
#  smp Mountain[/subkick/,[0,0]].tick(:sample), cutoff: 135, amp: 1.0, rpitch: (ring 0).look
  shader "vertex-settings", "line_loop", [@points, MAX_POINTSxx].min
  puts "ALIVE"
  sleep 1
end
