# c minor
# C, D, E♭, F, G, A♭, and B♭.

def harp(*args)
  params, opts = split_params_and_merge_opts_array(args)
  opts         = current_midi_defaults.merge(opts)
  n, vel = *params
  if n
    midi n,vel, *(args << {channel: 1})
  end
end

live_loop :start do
  tick
  score = (ing :c3, :f3, :Ab3,).look
  #harp score, 100, sus: 4.1

  sleep 1
  sleep 1

  with_transpose 12{
    midi score, 100, sus: 4.0, channel: 2
  }

  sleep 1
  sleep 1

end
