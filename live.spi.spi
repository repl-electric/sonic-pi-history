live_loop(:metro)do
  tick;
  cue("32") if look % 32 == 0
  cue("64") if look % 64 == 0
  puts "ARE YOU ALIVE"
  sleep 1

  # harp :FS3
#  10.times{harp_cc phase: rand, fx: rand}
end


live_loop :pattern, sync: :metro do
  tick
  sync "32"

  c1 = Dust[/clap/,1]
  if (s=(ring *%W{c1 _ _ _   _ _ _ _   _ _ _ _   _ _ _ _}).look) != _
    smp eval(s)
  end

  smp

  sleep 1/8.0
end
