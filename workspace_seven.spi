bar = 1.0

live_loop :bass do |idx|
      # 2.times{sync :whole}
with_synth :beep do
#with_fx :reverb do

#with_fx :slicer, phase: bar do
play (ring chord_degree(1, :A2, :major),
           chord_degree(4, :A2, :major),
           chord(:E3, :major7),

)[idx], decay: (ring bar/2.0, 
bar/2.0,
bar
)[idx], attack: 0.01, amp: 1.0
#end
#end
end
sleep (ring bar*2, bar*2, bar*2)[idx]
idx+=1
end