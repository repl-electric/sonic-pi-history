♥️,☠️=Straw
🐿=@slices
shader :shader, "wave.glsl", "rope.vert", "points", 100000
shader :iVertexCount, 1000000


live_loop :live do
  with_fx :none, mix: 0 do
    tick

    smp (ring *♥️ .slice(:Fs3).take(32).zip([_].cycle)).look


    sleep 1/8.0
  end
end
