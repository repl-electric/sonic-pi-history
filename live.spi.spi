live_loop :algo do
  tick
  if spread(1,4).look
    smp Mountain[/subkick/,[0,0]].tick(:sample), cutoff: 135, amp: 1.0, rpitch: (ring 0).look
  end

  smp e.slice("Fs3").look, amp: 4
  sleep 1/8.0
end