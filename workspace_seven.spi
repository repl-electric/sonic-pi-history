module Chimes
  def self.samples
    Dir["/Users/josephwilk/Dropbox/repl-electric/samples/12231__samulis__orchestral-chimes-samples/*"]
  end

  def self.notes(arg)
    self.samples.map{|note| 
      notes = note.match(/\d*__samulis__chimes-(.*)-(.*)-1.wav$/)
      if notes
        {notes[1].gsub(/-/,"") => note}
      end
    }
  end

  def self.note(n)
  end
end

puts Chimes.samples
puts Chimes.notes