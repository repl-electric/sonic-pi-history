live_loop(:metro)do
  tick;
  cue("32") if look % 32 == 0
  cue("64") if look % 64 == 0
  puts "ARE YOU ALIVE"
  sleep 1

  # harp :FS3
#  10.times{harp_cc phase: rand, fx: rand}
end


live_loop :pattern, sync: :metro do
  tick
  sync "32"

  smp Dust[/clap/,1]

  sleep 3
end
