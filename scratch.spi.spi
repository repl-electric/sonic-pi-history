def violin(*args)
  params, opts = split_params_and_merge_opts_array(args)
  opts         = current_midi_defaults.merge(opts)
  n, vel = *params
  if n.is_a?(Array)
    args =  args  << {sustain: n[1]}
    n = n[0]
  end
  if n
    midi n, vel, *(args << {port: :iac_bus_1} << {channel: 15})
  end
  violin_cc(opts)
end

live_loop :learn do
  tick
  violin :A2, form: 0.5, shape: 0.58*1, mul: 0.0
  sleep 1
end
