_ = nil
@polyrhythm = [3,2]

live_loop :foo do
  density(@polyrhythm.sort.first) { play degree(1, :FS3, :major); sleep 1 }
end

live_loop :bar, autocue: false do
  sync :foo
  density(@polyrhythm.sort.last) { play degree(5, :FS3, :major); sleep 1 }
end