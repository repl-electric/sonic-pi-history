♥️,☠️=Straw
🐿=@slices
shader :shader, "wave.glsl", "rope.vert", "points", 100000
shader :iVertexCount, 1000000



shader :iWave, 8.0
live_loop :live do
  with_fx :none, mix: 0 do
    tick

    if spread(1,4).look
      shader :decay, :iLineMode, rand(-100.0)
      shader :decay, :iBeat, rand*4, 0.001
      smp Mountain[/subkick/,[0,0]].tick(:sample), cutoff: 100, amp: 1.0
      if spread(1, 64).look
        at do
          sleep (1/8.0)*2
          smp Mountain[/subkick/,[0,0]].tick(:sample), cutoff: 135, amp: 1.0
        end
      end
    end

    if spread(1,8).look
      if spread(1,16).look
        shader :iR, rand*2
        shader :iG, rand*2
        shader :iB, rand*2

        smp Dust[/snare/,[1,1]].tick(:sample), cutoff: 135, amp: 1.0
      else
        end
    end



    with_fx(spread(7, 11).map{|s| s ? :pitch_shift : :bitcrusher}.look) do
      smp ♥️ .slice(:Fs3).take(32).look, amp: 4, cutoff: 90
    end

    sleep 1/8.0
  end
end
