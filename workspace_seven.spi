bar = 1.0

live_loop :bass do |idx|
      # 2.times{sync :whole}
with_synth :beep do
#with_fx :reverb do

#with_fx :slicer, phase: bar do
play (ring chord_degree(1, :A2, :major),
           chord_degree(4, :A2, :major),
           chord(:A2, :M7),

)[idx], decay: bar/2.0, attack: 0.1, amp: 1.0
#end
#end
end
sleep bar
idx+=1
end