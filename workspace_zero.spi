#midi_note_off :e3, channel: 3
def play_midi(*args)
  notes = args.first
  if notes.is_a?(SonicPi::Core::RingVector)||notes.is_a?(Array)
    notes.map{|n|
      midi *([n]+args[1..-1])
    }
  else
    midi *args
  end
end

live_audio "moog", input: 2
live_loop :testssssssz do
tick
smp Mountain[/subkick/,1], amp: 10
form chord(:Fs3, :m),(line 1,128,128).look, sustain: 5.8
sleep 1
end



puts chord(:Fs3, :m).is_a?(SonicPi::Core::RingVector)