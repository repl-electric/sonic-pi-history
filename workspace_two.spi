def degree_seq(pattern, root, scale=nil)
  if !scale
  scale = if(/[[:upper:]]/.match(root.to_s[0]))
      :major
    else
      :minor
    end
  end
  pattern.to_s.split("").map{|d| degree(d.to_i, root, scale)}
end

puts degree_seq(13322, :a2)