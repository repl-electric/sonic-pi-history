live_loop :testtest do
  tick
  moog (ring :fs1, :fs1, :gs1, :a1, :gs1, :a1, :a1, :cs2).look, 100, sustain: (knit 2,5, 1,3).look
  sleep (knit 2,4, 1,2).look
end

live_loop :drums, sync: :testtest do
  smp Mountain[/subkick/,1], amp: 1
  sleep 1
end
