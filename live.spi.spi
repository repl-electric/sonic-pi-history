live_loop :start do
  tick

  s = (knit 1/8.0, 31, 8,0).look
  f = spread(7,11).map{|s| s ? :reverb : :bitcrusher}
  with_fx(f.look) do
    if s > 1
      smp Straw.slice(:Fs3).look, amp: 4.0, finish_offset: 0.04
    else
      smp Straw.slice(:Fs3).look, amp: 4.0
    end
    sleep s
  end
end
